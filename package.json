{
  "name": "shopify-delivery-scheduler",
  "version": "1.0.0",
  "description": "Shopify apps for delivery scheduling and customer tag management",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd apps/admin-dashboard && pnpm install && pnpm run build",
    "build:tag-manager": "cd apps/shopify-tag-manager && pnpm install && pnpm run build",
    "dev:tag-manager": "cd apps/shopify-tag-manager && pnpm dev",
    "start:tag-manager": "cd apps/shopify-tag-manager && pnpm start"
  },
  "keywords": ["shopify", "delivery", "scheduler", "tags", "customer-segments"],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.12.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
